<Window x:Class="FoxLauncherWPF.VersionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        Icon="fox.ico"
        mc:Ignorable="d"
        Title="Выбор версии">
    <Grid>
        <Viewbox Stretch="Uniform">
            <Grid Width="460" Height="160">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
                    <ListBox Name="Version" Grid.Row="0" BorderThickness="0"/>
                    <Button Name="btnConfirm" Content="Подтвердить" Grid.Row="1" Margin="5" Height="41" BorderThickness="0"/>
                    <Image gif:ImageBehavior.AnimatedSource="fox.gif" Grid.Row="1" Margin="5"/>
                </StackPanel>
            </Grid>
        </Viewbox>
    </Grid>
</Window>